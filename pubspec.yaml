name: test_flutter
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  splashscreen:
  cloud_firestore: ^0.12.9+4
  firebase_auth: ^0.14.0+5
  toast: ^0.1.5

cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.3"

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/NUTTY LOGO.png"

flutter:

  uses-material-design: true
  assets:
    - assets/images/

